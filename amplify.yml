version: 1
frontend:
  phases:
    prebuild:
        commands: 
            - cd app
            - npm install
    build:
      commands:
        - node server.js
  artifacts:
    baseDirectory: /app
    files:
      - '**/*'
  cache:
    paths: []
